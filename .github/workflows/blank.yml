name: Release test

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Create new pre release
        run: npx lerna version prerelease --exact --force-publish --yes --create-release --conventional-commits
